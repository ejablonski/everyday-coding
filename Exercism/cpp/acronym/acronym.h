#pragma once

#include <string>
#include <vector>
#include <regex>

namespace acronym {
  using std::string, std::vector, std::regex, std::stringstream;

  string acronym(string _s);
}  // namespace acronym
